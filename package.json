{
  "name": "estudo-html-css-mazza",
  "version": "1.0.0",
  "scripts": {
    "dev": "browser-sync start --server 'src' --files '**/*' --no-cache"
  },
  "dependencies": {
    "jquery": "^3.7.1",
    "normalize.css": "^8.0.1",
    "prefixfree": "^1.0.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}
